# ESLint Setup

This project uses ESLint for code quality. To set up ESLint configuration, run:

```bash
npm install
```

The ESLint configuration will be automatically set up through the dev dependencies.

If you need to customize ESLint rules, create an `.eslintrc.cjs` file in the project root.

